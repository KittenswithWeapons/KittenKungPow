To run on local machine:

You must first start a local server.

To do so open a Command prompt window 

navigate to the project: I:\Users\abcjk\eclipse-workspace\KittenSmash

then run command:  npm run start  


Then open the local port in a web browser, usually http://localhost:5000/